import React from "react";
import styled from "styled-components";

const LandingOrders = styled.div`
  .logotyp {
    height: 24px;
    width: auto;
  }
`;

export const Orders = () => {
  return <LandingOrders></LandingOrders>;
};
